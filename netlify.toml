# Monorepo: Next.js app lives in apps/web
# Clerk + App Router need the Next.js runtime (not static `out/` export).

[build]
  base = "apps/web"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
